# Server Configuration
PORT=4000
NODE_ENV=development

# Database Configuration
# This is configured to connect to a local SQLite database for local dev environment
DATABASE_URL="file:./dev.db"

# Authentication
JWT_SECRET="buildtrack_jwt_secret_key_2026_super_secure"
JWT_EXPIRES_IN="7d"
